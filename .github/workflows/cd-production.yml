name: CD Production
on:
  workflow_dispatch:

jobs:
  deploy-prod:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -f docker/Dockerfile -t myacr.azurecr.io/gas:prod .
      - run: echo "Deploying to production..."
