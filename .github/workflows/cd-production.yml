name: CD - Production

on:
  workflow_dispatch:

jobs:
  deploy-prod:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build production image
        run: docker build -f docker/Dockerfile -t myregistry/gas:prod .

      - name: Push to registry (placeholder)
        run: echo "Replace with docker push and auth (use secrets)"

      - name: Deploy to production (placeholder)
        run: echo "Replace with production deploy steps (swap slots / k8s rollout)"
