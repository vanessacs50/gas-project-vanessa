name: CD Staging
on:
  workflow_dispatch:

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -f docker/Dockerfile -t myacr.azurecr.io/gas:staging .
      - run: echo "Deploying to staging environment..."
